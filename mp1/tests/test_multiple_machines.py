"""
Tests to make sure that things work between multiple machines
We have to test what if multiple clients run at the same time
What if one machine fails during the process
"""

def test_fail_machine():
    """
    Tests what happens if a machine fails or crashes during execution
    """
    assert(True)